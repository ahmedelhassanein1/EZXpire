{
  "name": "ezxpire",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:gemini": "node --env-file=.env --conditions=react-server scripts/testGemini.ts"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
