# Google Gemini (server-side only — used from Next.js API routes)
GEMINI_API_KEY=

# MongoDB Atlas
MONGODB_URI=

# Auth.js
AUTH_SECRET=
# Optional OAuth (if enabled)
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
